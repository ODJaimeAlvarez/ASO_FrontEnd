import { FilterFicherosPipe } from './filter-ficheros.pipe';

describe('FilterFicherosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFicherosPipe();
    expect(pipe).toBeTruthy();
  });
});
